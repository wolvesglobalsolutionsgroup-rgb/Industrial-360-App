name: CI Hardened Pipeline

on:
  push:
    branches: [ main, master, 'sprint/**' ]
  pull_request:
    branches: [ main, master, 'sprint/**' ]

jobs:
  security:
    name: Security Audit & Secret Scanning
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Security Audit (High Level)
        run: npm audit --audit-level=high || echo "Audit scan finished with findings"

      - name: Secret Scan (Gitleaks)
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  rules-check:
    name: Security Rules Integration Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java (for Firebase Emulators)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Run Firestore Rules Tests
        run: npm run test:rules

      - name: Run Storage Rules Tests
        run: npm run test:storage-rules

  build:
    name: Lint, Typecheck, Build & Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint code
        run: npm run lint

      - name: Typecheck Root (tsc --noEmit)
        run: npx tsc --noEmit

      - name: Typecheck Functions (tsc --noEmit)
        run: cd functions && npm ci && npx tsc --noEmit

      - name: Build application
        run: npm run build

      - name: Run Unit Tests
        run: npm run test:unit
